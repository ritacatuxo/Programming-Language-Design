
// * Global variables
int i; // address 0
double r; // address 2
char c; // address 6

// * Functions
int f(int pi, double pr) {
  double lr; // in line 12 is the first time we use lr. address is bp - 4
  int li;
  li = pi;
  lr = pr;
  return pi;
}

// * Main program
void main() {
  struct {
    int integer;
    char character;
  } pair;

  i = 3;
  c = 'a';
  r = 3.8;
  pair.integer = i;
  pair.character = c;
  f(i, r);
}


